{
  "name": "repos-automation",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "prestart": "npm run build",
    "start": "func start",
    "test": "jest"
  },
  "dependencies": {
    "@octokit/rest": "^16.35.0",
    "@octokit/webhooks": "^6.3.2",
    "@types/jest": "^24.0.23",
    "is-valid-github-event": "^0.1.0",
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0"
  },
  "devDependencies": {
    "@azure/functions": "^1.0.2-beta2",
    "typescript": "^3.7.2"
  },
  "jest": {
    "preset": "ts-jest",
    "testPathIgnorePatterns": ["/node_modules/", "dist"]
  }
}
